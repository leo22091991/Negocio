<%= form_with(model: salary_discount, local: true) do |form| %>
  <% if salary_discount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(salary_discount.errors.count, "error") %> prohibited this salary_discount from being saved:</h2>

      <ul>
      <% salary_discount.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
