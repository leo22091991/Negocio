<%= form_with(model: account_payment, local: true) do |form| %>
  <% if account_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account_payment.errors.count, "error") %> prohibited this account_payment from being saved:</h2>

      <ul>
      <% account_payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total %>
  </div>

  <div class="field">
    <%= form.label :current_account_id %>
    <%= form.text_field :current_account_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
