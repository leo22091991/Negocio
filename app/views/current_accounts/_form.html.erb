<%= form_with(model: current_account, local: true) do |form| %>
  <% if current_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(current_account.errors.count, "error") %> prohibited this current_account from being saved:</h2>

      <ul>
      <% current_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Apellido %>
    <%= form.text_field :last_name %>
  </div>

  <div class="field">
    <%= form.label :Nombre %>
    <%= form.text_field :first_name %>
  </div>

  <div class="field">
    <%= form.label :dni %>
    <%= form.text_field :dni %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

