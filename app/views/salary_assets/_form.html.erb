<%= form_with(model: salary_asset, local: true) do |form| %>
  <% if salary_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(salary_asset.errors.count, "error") %> prohibited this salary_asset from being saved:</h2>

      <ul>
      <% salary_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
